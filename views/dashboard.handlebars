    <div class="mb-5 pt-3">
        <a href="/dashboard/create/"><button id="create-post" class="button is-primary is-pulled-right">create post</button></a>
        
        <h2 class="title pb-4">Dashboard: Posts</h2><hr>
{{#if posts.length}}
    </div>
<!--If posts exist, option to edit-->
    {{#each posts as |post|}}
        {{> post-info post}}
        
            <a href="/dashboard/edit/{{post.id}}"><button class="button mb-5 ml-5 is-warning">Edit Your Post</button></a>
        </div>
    {{/each}}

<!--If no posts exist, no posts!-->
{{else}}
<h6 class="pb-4 has-text-centered is-italic">You have no posts!</h6>

{{/if}}


